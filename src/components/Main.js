import {Aside} from './Aside'
import {Section} from './Section'

export const Main = () => {
    return(
        <main className="Main">
            <Aside />
            <Section />
        </main>
    )
}